diff --git a/node_modules/@deepseek-ai/dsh-client-ui-sidebar-documentpreview/lib/client.js b/node_modules/@deepseek-ai/dsh-client-ui-sidebar-documentpreview/lib/client.js
--- a/node_modules/@deepseek-ai/dsh-client-ui-sidebar-documentpreview/lib/client.js
+++ b/node_modules/@deepseek-ai/dsh-client-ui-sidebar-documentpreview/lib/client.js
@@ -26913,6 +26913,35 @@
 		* Client plugin body: register the type, its dictionaries, its body, and its chip title.
 		* @param ctx - client root context carrying the registry, the slots, copy, and the Remote face.
 		*/
+        // Minke's embedded document face reuses these bodies and their native CSS.
+        // Drafts stay with Minke; HTML keeps DSH's packer, Remote reads and sandbox.
+        function registerMinkeDocumentPreview(ctx) {
+            const markdownT = ctx.locale.bind("documentMarkdown");
+            const htmlT = ctx.locale.bind("documentHtml");
+            const subscribe = listener => ctx.locale.subscribe(listener);
+            const getSnapshot = () => ctx.locale.getSnapshot();
+            const readRelated = (address, relativePath, signal) => {
+                const file = hostFileOf(address);
+                return ctx.remote.workspaceFiles.readRelated(file.sessionId, file.path, relativePath, signal);
+            };
+            function Body({ kind, text, resourceAddress }) {
+                react.useSyncExternalStore(subscribe, getSnapshot, getSnapshot);
+                const [signal, setSignal] = react.useState();
+                react.useEffect(() => {
+                    const lifetime = new AbortController();
+                    setSignal(lifetime.signal);
+                    return () => lifetime.abort();
+                }, [resourceAddress]);
+                const content = react.useMemo(() => kind === "markdown"
+                    ? { kind: "text", text, eof: true, pages: [{ offset: 1, text, lines: text.split("\n").length }] }
+                    : { kind: "bytes", data: new TextEncoder().encode(text) }, [kind, text]);
+                const useTabInfo = react.useMemo(() => () => ({ tab: { signal } }), [signal]);
+                if (kind === "markdown") return react_jsx_runtime.jsx(MarkdownBody, { content, t: markdownT });
+                if (!signal) return null;
+                return react_jsx_runtime.jsx(HtmlBody, { content, resourceAddress, readRelated, useTabInfo, t: htmlT });
+            }
+            ctx.effect(() => ctx.reflect.provide("minkeDocumentPreview", { Body }));
+        }
 		function apply(ctx) {
 			const previews = new DocumentPreviewRegistry();
 			const disposePreviews = ctx.reflect.provide("documentPreviews", previews);
@@ -26953,6 +26982,7 @@
 			apply$3(ctx);
 			apply$2(ctx);
 			apply$1(ctx);
+            registerMinkeDocumentPreview(ctx);
 		}
 		//#endregion
 		exports.apply = apply;
