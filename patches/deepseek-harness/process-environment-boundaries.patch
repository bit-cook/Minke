diff --git a/node_modules/@deepseek-ai/dsh-native-command/lib/index.js b/node_modules/@deepseek-ai/dsh-native-command/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-native-command/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-native-command/lib/index.js
@@ -5,2 +5,16 @@
 //#region lib/types/runner.js
+function externalCommandEnvironment() {
+	const environment = {
+		...process.env
+	};
+	for (const name of [
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]) for (const key of Object.keys(environment)) if (key.toUpperCase() === name) delete environment[key];
+	return environment;
+}
 /**
@@ -19,2 +33,3 @@
 		encoding: "utf8",
+		env: externalCommandEnvironment(),
 		signal,
diff --git a/node_modules/@deepseek-ai/dsh-sandbox-windows-acl/lib/runner.js b/node_modules/@deepseek-ai/dsh-sandbox-windows-acl/lib/runner.js
--- a/node_modules/@deepseek-ai/dsh-sandbox-windows-acl/lib/runner.js
+++ b/node_modules/@deepseek-ai/dsh-sandbox-windows-acl/lib/runner.js
@@ -156,5 +156,27 @@
 		}
+		const bootstrap = process.env.MINKE_NODE_BOOTSTRAP;
+		const embeddedExecutable = process.env.MINKE_NODE_EXECUTABLE;
+		const sameExecutable = typeof embeddedExecutable === "string" && (process.platform === "win32" ? parsed.command.toUpperCase() === embeddedExecutable.toUpperCase() : parsed.command === embeddedExecutable);
+		const childUsesEmbeddedNode = bootstrap !== void 0 && bootstrap !== "" && (parsed.command === process.execPath || sameExecutable);
+		for (const name of [
+			"ELECTRON_RUN_AS_NODE",
+			"MINKE_INTERACTIVE_NODE_OPTIONS",
+			"MINKE_INTERACTIVE_NODE_PATH",
+			"MINKE_NODE_BOOTSTRAP",
+			"NODE_OPTIONS",
+			"NODE_PATH"
+		]) for (const key of Object.keys(process.env)) if (key.toUpperCase() === name) delete process.env[key];
+		let childArgs = parsed.args;
+		if (childUsesEmbeddedNode) {
+			process.env.ELECTRON_RUN_AS_NODE = "1";
+			const hasBootstrap = childArgs.some((argument, index) => (argument === "--require" || argument === "-r") && childArgs[index + 1] === bootstrap || argument === `--require=${bootstrap}`);
+			if (!hasBootstrap) childArgs = [
+				"--require",
+				bootstrap,
+				...childArgs
+			];
+		}
 		const child = sandbox.spawn({
 			command: parsed.command,
-			args: parsed.args,
+			args: childArgs,
 			stdio: "inherit",
diff --git a/node_modules/@deepseek-ai/dsh-subprocess/lib/index.js b/node_modules/@deepseek-ai/dsh-subprocess/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-subprocess/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-subprocess/lib/index.js
@@ -50,4 +50,15 @@
 function scrubbedParentEnv() {
+	const ambientRuntimeControls = new Set([
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]);
 	const env = {};
-	for (const [key, value] of Object.entries(process.env)) if (value !== void 0 && !SENSITIVE_ENV_PATTERN.test(key) && !key.toUpperCase().startsWith("DSH_")) env[key] = value;
+	for (const [key, value] of Object.entries(process.env)) {
+		const normalized = key.toUpperCase();
+		if (value !== void 0 && !SENSITIVE_ENV_PATTERN.test(key) && !normalized.startsWith("DSH_") && !ambientRuntimeControls.has(normalized)) env[key] = value;
+	}
 	for (const [name, value] of Object.entries(proxyEnvironmentForChild())) if (value === void 0) Reflect.deleteProperty(env, name);
diff --git a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/index.js
@@ -1,2 +1,2 @@
-import { C as bindManagedProcess, D as createProcessInspector, E as validateSubprocessSpec, O as controlPipe, S as loadLinuxExecve, T as spawnSubprocess, _ as parseWindowsRunnerResult, c as runnerStdio, d as cleanupLinuxLaunchFiles, l as spawnRunnerInvocation, m as deserializeRunnerError, n as WINDOWS_RUNNER_SELECTION, o as runnerEnvironment, p as createLinuxLaunchFiles, s as runnerInvocationAvailable, u as targetEnvironment, w as childEnv, y as readLinuxStartupError } from "./runner-launch-BnjugUhn.js";
+import { embeddedNodeArguments, C as bindManagedProcess, D as createProcessInspector, E as validateSubprocessSpec, O as controlPipe, S as loadLinuxExecve, T as spawnSubprocess, _ as parseWindowsRunnerResult, c as runnerStdio, d as cleanupLinuxLaunchFiles, l as spawnRunnerInvocation, m as deserializeRunnerError, n as WINDOWS_RUNNER_SELECTION, o as runnerEnvironment, p as createLinuxLaunchFiles, s as runnerInvocationAvailable, u as targetEnvironment, w as childEnv, y as readLinuxStartupError } from "./runner-launch-BnjugUhn.js";
 import { prepareManagedProcessBinding } from "./output.js";
@@ -370,3 +370,12 @@
 };
+function scopeEnvironment(selection, invocation) {
+	const env = runnerEnvironment(selection, invocation);
+	delete env.ELECTRON_RUN_AS_NODE;
+	delete env.MINKE_NODE_BOOTSTRAP;
+	return env;
+}
 function scopeArgs(unitBase, invocation, argv) {
+	const env = {};
+	embeddedNodeArguments(invocation[0], [], env);
+	const bootstrapEnv = Object.entries(env).map(([key, value]) => `${key}=${value}`);
 	return [
@@ -379,2 +388,3 @@
 		"--",
+		...bootstrapEnv.length === 0 ? [] : ["/usr/bin/env", ...bootstrapEnv],
 		...invocation,
@@ -450,3 +460,3 @@
 		cwd: process.cwd(),
-		env: runnerEnvironment(files.requestPath, invocation),
+		env: scopeEnvironment(files.requestPath, invocation),
 		bindOwner: (direct) => new SystemdScopeOwner(`${unitBase}.scope`, startup, direct, internals.systemctl ?? "systemctl", internals.spawnSync ?? spawnSync, internals.systemctlQuery ?? querySystemctl, internals.sleep ?? sleepWithAbort),
@@ -478,3 +488,3 @@
 			cwd: process.cwd(),
-			env: runnerEnvironment(files.requestPath, invocation),
+			env: scopeEnvironment(files.requestPath, invocation),
 			stdio: runnerStdio(spec, false),
@@ -1131,4 +1141,6 @@
 		else {
+			const [program, ...args] = spec.argv;
+			const targetSpec = { ...spec, argv: [program, ...embeddedNodeArguments(program, args, env)] };
 			const binding = prepareManagedProcessBinding(this.internals);
-			handle = bindManagedProcess(spec, containmentMode === "linux-scope" ? launchLinuxScope(spec, env) : launchWindowsJob(spec, env), binding);
+			handle = bindManagedProcess(spec, containmentMode === "linux-scope" ? launchLinuxScope(targetSpec, env) : launchWindowsJob(targetSpec, env), binding);
 		}
@@ -1184,2 +1196,4 @@
 		const env = targetEnvironment(spec);
+		const args = embeddedNodeArguments(file, [...spec.argv.slice(1)], env);
+		const targetSpec = { ...spec, argv: [file, ...args] };
 		const options = {
@@ -1195,3 +1209,3 @@
 		const inspector = this.terminalInspector ?? createProcessInspector();
-		const scope = this.selectContainmentMode("terminal") === "linux-scope" ? prepareLinuxTerminalScope(spec, {
+		const scope = this.selectContainmentMode("terminal") === "linux-scope" ? prepareLinuxTerminalScope(targetSpec, {
 			...env,
@@ -1206,3 +1220,3 @@
 		try {
-			terminal = nodePty.spawn(scope?.command ?? file, scope?.args ?? [...spec.argv.slice(1)], options);
+			terminal = nodePty.spawn(scope?.command ?? file, scope?.args ?? args, options);
 		} catch (error) {
diff --git a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/runner-launch-BnjugUhn.js b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/runner-launch-BnjugUhn.js
--- a/node_modules/@deepseek-ai/dsh-subprocess-local/lib/runner-launch-BnjugUhn.js
+++ b/node_modules/@deepseek-ai/dsh-subprocess-local/lib/runner-launch-BnjugUhn.js
@@ -675,2 +675,10 @@
 	const env = scrubbedParentEnv();
+	for (const name of [
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]) for (const key of Object.keys(env)) if (key.toUpperCase() === name) delete env[key];
 	if (process.platform !== "win32") return {
@@ -686,2 +694,30 @@
 	return Object.fromEntries(entries);
+}
+function sameExecutable(left, right) {
+	if (typeof left !== "string" || typeof right !== "string") return false;
+	return process.platform === "win32" ? left.toUpperCase() === right.toUpperCase() : left === right;
+}
+function embeddedNodeArguments(program, args, env) {
+	const bootstrap = process.env.MINKE_NODE_BOOTSTRAP;
+	const executable = process.env.MINKE_NODE_EXECUTABLE;
+	if ((program !== process.execPath && !sameExecutable(program, executable)) || bootstrap === void 0 || bootstrap === "") return [...args];
+	for (const name of [
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]) for (const key of Object.keys(env)) if (key.toUpperCase() === name) delete env[key];
+	env.ELECTRON_RUN_AS_NODE = "1";
+	env.MINKE_NODE_BOOTSTRAP = bootstrap;
+	for (let index = 0; index < args.length; index += 1) {
+		const argument = args[index];
+		if ((argument === "--require" || argument === "-r") && args[index + 1] === bootstrap || argument === `--require=${bootstrap}`) return [...args];
+	}
+	return [
+		"--require",
+		bootstrap,
+		...args
+	];
 }
@@ -1008,5 +1044,6 @@
 	}
-	const child = (internals.spawn ?? spawn)(program, args, {
+	const env = controlEnvironment(childEnv(spec.env), spec.stdio.control);
+	const child = (internals.spawn ?? spawn)(program, embeddedNodeArguments(program, args, env), {
 		cwd: spec.cwd,
-		env: controlEnvironment(childEnv(spec.env), spec.stdio.control),
+		env,
 		stdio,
@@ -1284,8 +1321,11 @@
 function spawnRunnerInvocation() {
-	if ("pkg" in process) return [process.execPath];
+	const executable = process.env.MINKE_NODE_EXECUTABLE || process.execPath;
+	const bootstrapArgs = embeddedNodeArguments(executable, [], {});
+	if ("pkg" in process) return [executable, ...bootstrapArgs];
 	/* v8 ignore next -- built-artifact smoke imports the emitted JavaScript runner entry;
 	* source-unit coverage cannot change import.meta.url. */
-	if (extname(fileURLToPath(import.meta.url)) !== ".ts") return [process.execPath, fileURLToPath(import.meta.resolve("@deepseek-ai/dsh-subprocess-local/runner"))];
+	if (extname(fileURLToPath(import.meta.url)) !== ".ts") return [executable, ...bootstrapArgs, fileURLToPath(import.meta.resolve("@deepseek-ai/dsh-subprocess-local/runner"))];
 	return [
-		process.execPath,
+		executable,
+		...bootstrapArgs,
 		"--import",
@@ -1323,2 +1363,3 @@
 	}
+	embeddedNodeArguments(invocation?.[0] ?? process.execPath, [], env);
 	return {
@@ -1502,2 +1543,2 @@
 //#endregion
-export { bindManagedProcess as C, createProcessInspector as D, validateSubprocessSpec as E, controlPipe as O, loadLinuxExecve as S, spawnSubprocess as T, parseWindowsRunnerResult as _, resolveWindowsExecutable as a, serializeRunnerError as b, runnerStdio as c, cleanupLinuxLaunchFiles as d, consumeLinuxLaunchRequest as f, linuxLaunchFilesFromLocator as g, isWindowsTerminateRequest as h, parseRunnerTargetArgv as i, spawnRunnerInvocation as l, deserializeRunnerError as m, WINDOWS_RUNNER_SELECTION as n, runnerEnvironment as o, createLinuxLaunchFiles as p, consumeRunnerSelection as r, runnerInvocationAvailable as s, SUBPROCESS_RUNNER_ENV as t, targetEnvironment as u, parseWindowsStartRequest as v, childEnv as w, writeLinuxStartupError as x, readLinuxStartupError as y };
+export { embeddedNodeArguments, bindManagedProcess as C, createProcessInspector as D, validateSubprocessSpec as E, controlPipe as O, loadLinuxExecve as S, spawnSubprocess as T, parseWindowsRunnerResult as _, resolveWindowsExecutable as a, serializeRunnerError as b, runnerStdio as c, cleanupLinuxLaunchFiles as d, consumeLinuxLaunchRequest as f, linuxLaunchFilesFromLocator as g, isWindowsTerminateRequest as h, parseRunnerTargetArgv as i, spawnRunnerInvocation as l, deserializeRunnerError as m, WINDOWS_RUNNER_SELECTION as n, runnerEnvironment as o, createLinuxLaunchFiles as p, consumeRunnerSelection as r, runnerInvocationAvailable as s, SUBPROCESS_RUNNER_ENV as t, targetEnvironment as u, parseWindowsStartRequest as v, childEnv as w, writeLinuxStartupError as x, readLinuxStartupError as y };
diff --git a/node_modules/@deepseek-ai/dsh-web-app/lib/index.js b/node_modules/@deepseek-ai/dsh-web-app/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-web-app/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-web-app/lib/index.js
@@ -43,2 +43,15 @@
 const ALL_INTERFACES_HOST = "0.0.0.0";
+function browserWorkerEnvironment() {
+	const environment = scrubbedParentEnv();
+	for (const name of [
+		"ELECTRON_RUN_AS_NODE",
+		"MINKE_INTERACTIVE_NODE_OPTIONS",
+		"MINKE_INTERACTIVE_NODE_PATH",
+		"MINKE_NODE_BOOTSTRAP",
+		"NODE_OPTIONS",
+		"NODE_PATH"
+	]) for (const key of Object.keys(environment)) if (key.toUpperCase() === name) delete environment[key];
+	environment.ELECTRON_RUN_AS_NODE = "1";
+	return environment;
+}
 const BROWSER_OPENER_MODULE = import.meta.resolve("open");
@@ -46,2 +59,5 @@
 try {
+  delete process.env.ELECTRON_RUN_AS_NODE
+  delete process.env.NODE_OPTIONS
+  delete process.env.NODE_PATH
   const { default: open } = await import(${JSON.stringify(BROWSER_OPENER_MODULE)})
diff --git a/node_modules/@deepseek-ai/dsh-ptc-runtime-node/lib/index.js b/node_modules/@deepseek-ai/dsh-ptc-runtime-node/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-ptc-runtime-node/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-ptc-runtime-node/lib/index.js
@@ -942,8 +942,13 @@
 			if (settled) return await result.promise;
 			const packaged = "pkg" in process && this.config.bootstrapPath === void 0;
 			const heapFlag = `--max-old-space-size=${this.config.maxOldGenerationSizeMb}`;
+			const nodeBootstrap = process.env.MINKE_NODE_BOOTSTRAP;
+			const embeddedExecutable = process.env.MINKE_NODE_EXECUTABLE;
+			const embeddedNode = typeof nodeBootstrap === "string" && nodeBootstrap !== "" && (executable === process.execPath || typeof embeddedExecutable === "string" && (process.platform === "win32" ? executable.toUpperCase() === embeddedExecutable.toUpperCase() : executable === embeddedExecutable));
 			const argv = [
+				...embeddedNode && process.platform !== "win32" ? ["/usr/bin/env", "ELECTRON_RUN_AS_NODE=1", `MINKE_NODE_BOOTSTRAP=${nodeBootstrap}`] : [],
 				executable,
+				...embeddedNode ? ["--require", nodeBootstrap] : [],
 				...packaged ? [] : [heapFlag],
 				...bootstrapArgs(this.ctx.fs, this.config, this.config.maxMessageBytes)
 			];
@@ -954,6 +959,10 @@
 			if (settled) return await result.promise;
 			if (confined !== void 0) sandbox.enforcement = confined.enforcement;
 			const env = Object.fromEntries(Object.keys(process.env).filter((key) => !STARTUP_ENVIRONMENT_NAMES.has(key.toUpperCase())).map((key) => [key, void 0]));
+			if (embeddedNode) {
+				env.ELECTRON_RUN_AS_NODE = "1";
+				env.MINKE_NODE_BOOTSTRAP = nodeBootstrap;
+			}
 			if (packaged) {
 				env.DSH_PTC_RUNTIME_NODE = "1";
 				env.NODE_OPTIONS = heapFlag;
