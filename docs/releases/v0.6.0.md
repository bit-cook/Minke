# Minke v0.6.0

Minke 是基于 DeepSeek Harness（DSH）的本地优先桌面 Agent 工作空间，把对话、项目文件、终端和浏览器放在一起，也能从手机或聊天应用远程使用。

## 功能概览

- **Agent 工作流**：使用 DSH 的智能体预设、技能、计划、目标和子代理；浏览长会话的轮次目录、导出日志、安排会话内的定时跟进。
- **共同控制浏览器**：让 Agent 在可见标签页中操作网页，随时接管或交还控制，也可以把页面标注发送到对话。
- **文件与终端**：在对话旁编辑文件、查看源码和 Diff、预览 Markdown / HTML 草稿，并使用项目终端。
- **云端与本地模型**：支持 DSH 模型提供方和自定义端点，集成 LM Studio、Ollama 的模型发现、自动启动与运行状态。
- **远程协作**：支持手机 Web / PWA、微信、Telegram 和 Discord；Web 访问可使用 Tailscale 或 Cloudflare Access。
- **桌面与插件**：提供插件管理、安全模式、快捷键、主题同步和应用更新；会话、设置与浏览历史保存在本机。

## 本次更新

- **升级至 `dsh-v0.1.5-rc.2`**：支持任意类型附件上传、模型交付文件，以及 Markdown、代码、HTML、PDF 和图片预览。改进长会话、消息队列、模型配置与反馈提交；支持在模型明确声明兼容时动态更新系统提示词并保留 KV Cache。
- **统一右侧 Tabs**：采用 DSH 的标签页、分屏、浮动和全屏容器，兼容 Minke 的文件、终端、网页、历史和插件标签。新增菜单区分 DSH 与 Minke，保留 Minke 文件管理风格和独立底栏。
- **完善本地模型服务**：LM Studio 与 Ollama 的开关、错误和状态独立展示；修复 Ollama 没有已下载模型时无法正常启动的问题。
- **打磨日常操作**：优化 macOS 拖动区域、按钮、滚动和浏览历史；修复标签图标与加载动画。文件预览可切换效果、源码和 Diff，关闭按钮始终可见。
- **支持慢加载重试**：页面加载失败后可重新加载，复用已启动的 DSH 后端，并支持调整等待时间。

## 升级说明

- **Session V3**：受支持的历史会话会迁移为新的日志文件，原日志保留。旧版本无法读取 V3 的新增记录，升级前建议备份当前数据目录。
- Codex / Claude Code 子代理插件需要另行安装并启用；DSH Agent Teams 仍是需要显式配置的实验功能。
- 第三方插件需兼容 DSH 的 Agent、Inbox 和面板 API 变更。安全模式会跳过第三方插件启动，并保留安装，便于排查与恢复。

感谢 DeepSeek Harness 的贡献者，以及帮助测试和反馈 Minke 的用户。

[中文介绍](https://github.com/lencx/Minke/blob/v0.6.0/README.zh-CN.md) · [English overview](https://github.com/lencx/Minke/blob/v0.6.0/README.md) · [DSH 更新记录](https://github.com/deepseek-ai/deepseek-harness/compare/dsh-v0.1.3-alpha.1...dsh-v0.1.5-rc.2)
